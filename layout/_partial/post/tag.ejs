<% if (post.tags && post.tags.length){
  var className = (typeof class_name === 'undefined' ? 'article-tag' : class_name);
%>
  <%- list_tags(post.tags, {
    show_count: false,
    class: className
  }) %>
<% } %>